apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql
  namespace: study
  labels:
    app: mysql
data:
  master.cnf: |
    [mysqld]
    log-bin
  slave.cnf: |
    # Apply this config only on slaves.
    [mysqld]
    super-read-only
